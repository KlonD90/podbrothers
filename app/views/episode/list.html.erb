<% if @episodes.blank? %>
    Not exists
<% else %>
    <% @episodes.each do |e| %>
        <h1><%= e.title %></h1>
        <img src="<%= e.image.url(:thumb) %>"/>
        <div class="podcast__description">
          <%= e.description %>
        </div>
    <% end %>
<% end %>